DASHBOARD_USER=abby
DASHBOARD_PASSWORD=change-me
DATA_DIR=data
SEED_IF_EMPTY=0

# Everything else — OpenRouter key/model, Gmail sender + App Password, the auto-send
# schedule, recipients, and PDF design — is configured from the dashboard's Settings
# page and stored on the Client row, not here.
